import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CambiocorrectoPage } from './cambiocorrecto.page';

describe('CambiocorrectoPage', () => {
  let component: CambiocorrectoPage;
  let fixture: ComponentFixture<CambiocorrectoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CambiocorrectoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
